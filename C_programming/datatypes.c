#include<stdio.h>
#include<conio.h>
void main(){
    int a = 10 ;
    printf("value of a = %d", a );  // %d is used for integers 

    float b = 4.9 ;
    printf("\n value of b = %f", b); // /n is used for new line and %f is used for float numbers

    char c = "siddharth";
    printf("/n value of c = %s", c ); // %s is used for strings 

    getch(); //wait for a key press

    return 0 ; // return 0 indicates the successful execicution of the program and it is mostly used in int main() not used in void main()
    
    
}
#include<stdio.h>

void main(){
    int count;
    // for(count = 1 ; count<51;count++){
    //     // printf("\nhello");
    //     printf("\n%d",count);
        for(count = 10; count>0; count--){
            printf("\n%d", count);
    }
}